apiVersion: v1
kind: ConfigMap
metadata:
  name: wger-settings
  namespace: fitness
data:
  settings_local.py: |
    import os

    # Force PostgreSQL - This overrides EVERYTHING
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.postgresql',
            'NAME': 'wger',
            'USER': 'wger',
            'PASSWORD': os.environ.get('WGER_DB_PASSWORD') or os.environ.get('DB_PASSWORD'),
            'HOST': '127.0.0.1',
            'PORT': '5432',
        }
    }

    DEBUG = False
    ALLOWED_HOSTS = ['fitness.mvgates.com', 'localhost', '127.0.0.1']
    CSRF_TRUSTED_ORIGINS = ['https://fitness.mvgates.com']

    # WhiteNoise is mandatory for CSS/JS in production
    # We use the correct middleware order
    MIDDLEWARE = [
        'django.middleware.security.SecurityMiddleware',
        'whitenoise.middleware.WhiteNoiseMiddleware',
        'django.contrib.sessions.middleware.SessionMiddleware',
        'django.middleware.common.CommonMiddleware',
        'django.middleware.csrf.CsrfViewMiddleware',
        'django.contrib.auth.middleware.AuthenticationMiddleware',
        'django.contrib.messages.middleware.MessageMiddleware',
        'django.middleware.clickjacking.XFrameOptionsMiddleware',
    ]

    STATIC_ROOT = '/home/wger/static'
    STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'
