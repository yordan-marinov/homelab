apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-mvgates-cert
  namespace: monitoring
spec:
  secretName: grafana-mvgates-tls
  dnsNames:
    - grafana.mvgates.com
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
