apiVersion: apps/v1
kind: Deployment
metadata:
  name: rook-ceph-tools
  namespace: rook-ceph
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rook-ceph-tools
  template:
    metadata:
      labels:
        app: rook-ceph-tools
    spec:
      serviceAccountName: rook-ceph-system   # matches existing SA
      containers:
        - name: rook-ceph-tools
          image: quay.io/ceph/ceph:v19.2.0
          command: ["/bin/bash", "-c", "--"]
          args: ["sleep infinity"]
          tty: true
          stdin: true
          volumeMounts:
            - name: ceph-conf
              mountPath: /etc/ceph
            - name: ceph-client-admin
              mountPath: /etc/ceph/keyring
              subPath: keyring
      volumes:
        - name: ceph-conf
          configMap:
            name: rook-ceph-mon-endpoints  # matches your existing CM
        - name: ceph-client-admin
          secret:
            secretName: rook-ceph-admin-keyring

