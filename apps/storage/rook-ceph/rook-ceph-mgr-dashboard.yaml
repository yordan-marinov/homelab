apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-mgr-dashboard
  namespace: rook-ceph
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8443
  selector:
    app: rook-ceph-mgr

