apiVersion: apps/v1
kind: Deployment
metadata:
  name: zeroclaw-sre
  namespace: management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zeroclaw-sre
  template:
    metadata:
      labels:
        app: zeroclaw-sre
    spec:
      serviceAccountName: zeroclaw-sre-sa
      imagePullSecrets: [{ name: zeroclaw-ghcr-pull-secret }]

      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000

      initContainers:
        - name: volume-permissions-fixer
          image: busybox
          securityContext:
            runAsUser: 0
          command: ["sh", "-c", "chown -R 1000:1000 /work && chmod -R 775 /work"]
          volumeMounts:
            - name: workspace
              mountPath: /work

      containers:
        # --- THE SRE AGENT ---
        - name: agent
          image: ghcr.io/mvg-labs/zeroclaw-sre:latest
          imagePullPolicy: Always
          env:
            - name: OPENAI_API_KEY
              valueFrom: { secretKeyRef: { name: zeroclaw-app-secrets, key: virtual_key } }
            - name: OPENAI_API_BASE
              value: "http://litellm-service.ai-core.svc.cluster.local:4000/v1"
            - name: HOME
              value: "/work"
            - name: SRE_STATIC_TOKEN
              value: "mvg-labs-sre-static-token-unique-2024"
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -e
              # Ensure the internal directory exists
              mkdir -p /work/.zeroclaw

              echo "[1/3] Initializing ZeroClaw..."
              # Onboard to create the database
              zeroclaw onboard --api-key $OPENAI_API_KEY --provider openai --model "homecrew-sre-brain" --force

              echo "[2/3] Writing Master Config to /work/.zeroclaw/config.toml..."
              # WE WRITE TO THE EXACT PATH THE LOG REPORTED
              cat <<EOF > /work/.zeroclaw/config.toml
              [gateway]
              host = "0.0.0.0"
              port = 18789
              allow_public_bind = true
              api_token = "$SRE_STATIC_TOKEN"
              pairing_enabled = false

              [llm]
              provider = "openai"
              model = "homecrew-sre-brain"
              api_key = "$OPENAI_API_KEY"
              base_url = "$OPENAI_API_BASE"

              [workspace]
              # Force the agent to use our Git repo as its workspace
              base_path = "/work/repo/homelab/apps/management/zeroclaw"
              identity_path = "/work/repo/homelab/apps/management/zeroclaw/core/IDENTITY.md"
              soul_path = "/work/repo/homelab/apps/management/zeroclaw/core/SOUL.md"
              policies_path = "/work/repo/homelab/apps/management/zeroclaw/core/POLICIES.md"
              user_path = "/work/repo/homelab/apps/management/zeroclaw/core/USER.md"

              [skills]
              # Point to our dynamic Git-Sync skills folder
              paths = ["/work/repo/homelab/apps/management/zeroclaw/skills"]
              EOF

              echo "[3/3] Starting ZeroClaw SRE Daemon..."
              # ZeroClaw will now pick up /work/.zeroclaw/config.toml by default
              exec zeroclaw daemon
          volumeMounts:
            - name: workspace
              mountPath: /work
          ports:
            - containerPort: 18789

        # --- THE SYNC SIDECAR ---
        - name: git-sync
          image: registry.k8s.io/git-sync/git-sync:v4.2.3
          securityContext:
            runAsUser: 1000
          args:
            - --repo=https://github.com/mvg-labs/homelab.git
            - --branch=main
            - --wait=60
            - --root=/work/repo
            - --link=homelab
          env:
            - name: GIT_SYNC_PASSWORD
              valueFrom: { secretKeyRef: { name: zeroclaw-app-secrets, key: github_token } }
            - name: GIT_SYNC_USERNAME
              value: "mvg-labs-bot"
          volumeMounts:
            - name: workspace
              mountPath: /work

      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: zeroclaw-workspace-pvc
