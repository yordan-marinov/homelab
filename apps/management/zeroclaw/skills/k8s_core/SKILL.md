# Skill: Kubernetes Management
Allows the agent to monitor and manage the local cluster.

## Capabilities:
- List nodes, pods, services, and ingresses.
- Check pod logs and describe resources.
- Audit cluster health.

## Usage:
The agent can execute shell commands using `kubectl`.
