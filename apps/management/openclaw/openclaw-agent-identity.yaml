apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openclaw-agent-identity
  namespace: management

spec:
  refreshInterval: 1h

  secretStoreRef:
    name: infisical-store
    kind: ClusterSecretStore

  target:
    name: openclaw-agent-secret
    creationPolicy: Owner

  data:

    - secretKey: github_token
      remoteRef:
        key: /agents/homecrew-sre/GITHUB_TOKEN

    - secretKey: git_ssh_private_key
      remoteRef:
        key: /agents/homecrew-sre/GIT_SSH_PRIVATE_KEY

    - secretKey: git_user_name
      remoteRef:
        key: /agents/homecrew-sre/GIT_USER_NAME

    - secretKey: git_user_email
      remoteRef:
        key: /agents/homecrew-sre/GIT_USER_EMAIL

